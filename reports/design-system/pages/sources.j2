<section class="page page--sources">
  <h2>{{ content.pages.sources.title or "Sources and Citations" }}</h2>
  {% if content.sources %}
  <p class="section-lede">{{ content.pages.sources.lede }}</p>
  <table class="source-table">
    <colgroup>
      <col class="col-ref" />
      <col class="col-label" />
      <col class="col-cited" />
    </colgroup>
    <thead>
      <tr>
        <th>Ref</th>
        <th>Source</th>
        <th>Findings</th>
      </tr>
    </thead>
    <tbody>
      {% for s in content.sources %}
      <tr>
        <td class="mono">{{ s.ref }}</td>
        <td>
          <span class="source-table__label">{{ s.label }}</span>
          {% if s.url %}<span class="source-table__url">{{ s.url }}</span>{% endif %}
        </td>
        <td class="num">{{ s.cited_by }}</td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
  {% else %}
  <p class="section-lede">{{ content.pages.sources.empty }}</p>
  {% endif %}
</section>
