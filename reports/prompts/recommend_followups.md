# Remediation / follow-up recommendations

Given scored findings, propose prioritized follow-ups using methods:

`reword` | `retime` | `redact` | `restructure`

Each item: action, method, linked `claim_id`s, rationale.

## Data

{{ report_data_json }}
