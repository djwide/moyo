"""claudeExposureBuild — an alternative Exposure Snapshot + one-page builder.

A self-contained report product (own charts, templates, and CSS) that renders
from the same scored run data (``report_data.json``) as the default builder.
Outputs land under ``reports/build/<run-id>/claude/output/`` so both products
sit side by side for comparison.
"""
